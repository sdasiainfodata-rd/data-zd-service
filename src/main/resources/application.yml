eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9090
spring:
  application:
    name: datacenter
  datasource:
        username: admin
        password: sdyx2019
        url: jdbc:mysql://47.105.40.83:20886/eladmin?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.asiainfo.security.entity
#showSql
logging:
  level:
    com:
      example:
        mapper : debug
#//ghghhgh
#collection
mongodb:
  data:
    collection1: news_data_permission
    consumers:
      consumers1: user_dp
    roles: roles
    permissions: permissions
#jwt
jwt:
  secret: mySecret
  expiration: 604800000
  header: Authorization